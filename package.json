{
  "name": "tryfoundry-site",
  "version": "1.0.0",
  "private": true,
  "description": "Marketing site for Foundry. Eleventy is used only for the /blog section; root pages remain hand-written HTML.",
  "scripts": {
    "build": "eleventy",
    "dev": "eleventy --serve --port=4001",
    "watch": "eleventy --watch"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "@11ty/eleventy-plugin-rss": "^2.0.2"
  }
}
